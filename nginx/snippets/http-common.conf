# http{}-level settings shared by both configs.
include       /etc/nginx/mime.types;
default_type  application/octet-stream;

sendfile           on;
tcp_nopush         on;
keepalive_timeout  65;
server_tokens      off;

# Uploads go through Express (multer -> Cloudinary); backup restore allows 50 MB.
client_max_body_size 50m;

gzip on;
gzip_vary on;
gzip_proxied any;
gzip_min_length 1024;
gzip_types text/plain text/css application/json application/javascript
           text/xml application/xml image/svg+xml;

log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                '$status $body_bytes_sent "$http_referer" '
                '"$http_user_agent" rt=$request_time';
access_log /var/log/nginx/access.log main;

upstream nextjs  { server client:3000; keepalive 16; }
upstream express { server server:5001; keepalive 16; }

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      '';
}

add_header X-Content-Type-Options nosniff always;
add_header Referrer-Policy strict-origin-when-cross-origin always;
add_header X-Frame-Options SAMEORIGIN always;
